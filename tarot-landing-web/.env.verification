# Loaded ONLY when vite runs with --mode verification (see .claude/launch.json).
# Plain `npm run dev` keeps using .env untouched. Points the cockpit at the
# LOCAL docker backend so verification writes never reach production.
VITE_API_URL=http://localhost:8000
